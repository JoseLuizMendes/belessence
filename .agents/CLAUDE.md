---
template: "Niche CLAUDE"
version: 1.0
status: "Ativo"
tags:
  - niche-claude
  - per-diretorio
nicho: ".agents"
escopo: "Hub de skills + outputs + inspirations para agentes IA"
---

# .agents

> **Nota de Uso:** Conteúdo curado para IAs que entrarem no repo. Sub-pastas: `skills/` (instaladas), `output/` (auditorias geradas), `inspiration/` (referências de design).
>
> Gerado por `tools/generate-claude-md.js` em 2026-05-30 conforme regra **R8** (`[[CLAUDE]]` raiz + `[[Preferencias Dev#4. CLAUDE.md Universal]]`).

---

## Escopo do Diretório

Conteúdo curado para IAs que entrarem no repo. Sub-pastas: `skills/` (instaladas), `output/` (auditorias geradas), `inspiration/` (referências de design).

---

## Diretrizes Específicas

- Sub-pastas: `skills/` (instaladas via skill manager), `output/` (auditorias históricas), `inspiration/` (referências criativas).
- Cada arquivo é orientação para a IA — não código.

---

## Stack Local

Conforme `[[Preferencias Dev]]`.

---

## Testes

Vitest + Playwright conforme `[[Preferencias Dev]]`.

---

## Dependências Permitidas

N/A.

---

## Referências

- `CLAUDE.md` global do projeto (raiz)
- `[[Preferencias Dev]]` — stack aprovada
- `[[Niche CLAUDE Template]]` — template canon
