// Stub para o módulo virtual `server-only` do Next.js durante testes Vitest.
// O `server-only` original aborta o build se for importado em código client;
// em testes, queremos simplesmente permitir o import sem efeito colateral.
export {};
